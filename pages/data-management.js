export default function DataManagement() {
  return (
    <div style={{ padding: '20px' }}>
      <h1>Data Management</h1>
      <p>This page will allow searching, filtering, and CSV import/export for leads.</p>
      {/* Future: Add search, filter, sort, and CSV import/export functionality */}
    </div>
  );
}
